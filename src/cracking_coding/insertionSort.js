function insertionSort(arr){
    console.log(arr);
    var temp=[];
    for(var i=0;i<arr.length;i++){
        if(temp=[]||)
        temp.unshift(arr[i]);

    }
}
var arr=[9,3,6,4];

console.log(insertionSort(arr));